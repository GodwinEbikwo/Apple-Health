import React from "react";
import { View, Text } from "react-native";

interface Props {}

const Info = (props: Props) => {
  return (
    <View>
      <Text>Hi I am info</Text>
    </View>
  );
};

export default Info;
